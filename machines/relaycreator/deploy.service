[Unit]
Description=relaycreator deployment

# TODO: needs to build, migrate prisma, restart app
[Service]
ExecStart=/usr/bin/npm start
WorkingDirectory=/app
Restart=always
User=root
Environment=NODE_ENV=production

[Install]
WantedBy=multi-user.target